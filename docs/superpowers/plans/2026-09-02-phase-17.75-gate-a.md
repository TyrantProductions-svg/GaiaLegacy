# Phase 17.75A — Contract and Dependency Admission Plan

> Execute only Gate A with TDD. User authorization overrides workflow defaults:
> no Git staging/commit/push, no linked worktree, no viewer, no Blender edits.

**Goal:** prove a bounded, offline, tools-only JglTF 3.0.1 admission boundary.
**Architecture:** independent tools source sets; strict streaming preflight;
JglTF non-resolving asset mapping; immutable receipt, not a full validator.
**Stack:** Java 17 API/bytecode, Gradle wrapper, JUnit, JglTF and its Jackson tree.
Baseline: `d3a219767b2a16c6f4e5f7514019c56ca9cf6cf7`.

All commands use the current process's JDK and, where necessary, the known
process-local JAVA_TOOL_OPTIONS selector fallback. No absolute JDK config.

## Task 1 — Source-set and dependency admission

Files: `tools/build.gradle`,
`tools/src/modelInspectorTest/java/com/gaia/tools/model/DependencyAdmissionTest.java`.

1. Add only isolated test/source-set harness and test dependencies.
2. RED: `DependencyAdmissionTest` cannot load the required JglTF classes.
   Run `./gradlew.bat :tools:modelInspectorTest --tests '*DependencyAdmissionTest' --console=plain --no-daemon`.
3. GREEN: add exact `de.javagl:jgltf-model:3.0.1` only to isolated inspector
   implementation. Resolve and record the actual Jackson transitive selection.
4. Prove the inspector classpath cannot load engine/game/LWJGL, and new compile
   tasks use `--release 17`. Do not put inspector output into tools' main jar.
5. Record complete dependency tree and bytecode compatibility (no full-suite run).

## Task 2 — Bounded container snapshot

Files: `GlbPreflight.java`, `PreflightException.java` under
`tools/src/modelInspector/java/com/gaia/tools/model/`; `GlbPreflightTest.java`
and `GlbFixtures.java` under the isolated test source set.

1. RED: absent API compile failure is expected. Test valid tiny GLB framing,
   malformed magic/version/length, truncated/unknown/misordered chunks,
   overflow, JSON/file size bounds, and immutable same-snapshot consumption.
2. Run `./gradlew.bat :tools:modelInspectorTest --tests '*GlbPreflightTest' --console=plain --no-daemon`.
3. GREEN: bounded InputStream read, checked arithmetic, restrictive chunk layout,
   immutable owned bytes. Caller retains stream ownership. No Path/URI reader.
4. Re-run tests plus Task 1; no semantic geometry/image interpretation.

## Task 3 — Security JSON preflight

Files: same preflight plus `GlbJsonSecurityTest.java`.

1. RED: framed GLBs with URI fields, extensions, duplicate keys, wrong version,
   malformed UTF-8/JSON, extra values, or depth/token/string/number excess must
   reject with typed bounded errors; escaped URI keys must not bypass checks.
2. Run `./gradlew.bat :tools:modelInspectorTest --tests '*GlbJsonSecurityTest' --console=plain --no-daemon`.
3. GREEN: Jackson streaming tokenization with strict constraints and independent
   limits. Complete this pass before JglTF; never rely on its model decoder for
   security. Do not implement a complete glTF schema or semantic validator.
4. Re-run complete isolated suite.

## Task 4 — Real no-reference decode and boundary evidence

Files: `GlbAdmission.java`, `GlbAdmissionTest.java`, trusted project-owned triangle
fixture builder, dependency/license documentation and notices.

1. RED: absent admission API compile failure expected; valid triangle produces
   bounded admission receipt; URI/extension hidden late in otherwise malformed
   JglTF content must reject at preflight, not enter semantic decoding.
2. Run `./gradlew.bat :tools:modelInspectorTest --tests '*GlbAdmissionTest' --console=plain --no-daemon`.
3. GREEN: JglTF `readWithoutReferences(InputStream)` AFTER preflight; no returned
   mutable library model and no untrusted `GltfModels.create`. Bounded decoder
   failure translation; no unbounded diagnostics or input echo.
4. A trusted triangle compatibility test may call `GltfModels.create` and inspect
   decoded coordinates/normals. This does not approve arbitrary GLBs.
5. Verify isolated dependency graph and unchanged main/runtime dependency graphs;
   run full isolated suite and existing `:tools:test`; verify CI `check` wiring.
6. Hash exact resolved artifacts and licenses; document source tag/commit versus
   source-byte/artifact SHA-256; retain complete applicable notices. Final audit,
   scoped review, handoff. STOP; do not proceed to Gate B.

## Final evidence

Task 4 evidence-driven amendment: the planned `GltfAssetReader` path failed the
bounded rejection test: `buffers: "PRIVATE_MARKER"` logged the marker and returned
an asset despite the configured throwing callback. Do not weaken this assertion.
Use strict Jackson binding to public JglTF v2 DTOs and `GltfAssetV2` instead. This
keeps the dependency, the preflight ordering and Gate A scope; no full validator
or third-party patch. Record this limitation in the admission handoff.

`./gradlew.bat :tools:modelInspectorTest :tools:test --console=plain --no-daemon`

`./gradlew.bat :tools:dependencies --configuration modelInspectorRuntimeClasspath --console=plain --no-daemon`

Use a dry-run of `:tools:check` for wiring; inspect engine/game/tools main runtime
classpath reports for isolation. `git diff --check`, file/status/hash audits.
Do not rerun the historical multi-hour repository suite for this isolated gate.
Native macOS and a real JDK17 run must be labeled NOT RUN if unavailable; a
`--release 17` compile plus dependency bytecode audit is narrower evidence.
