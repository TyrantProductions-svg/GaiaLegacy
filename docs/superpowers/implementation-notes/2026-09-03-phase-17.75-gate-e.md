# Phase 17.75 Gate E — end-to-end local closure

Status: final local integration and controller-authorized contract-closure evidence
collected on 2026-09-03. Gates A/B/C/D were controller-approved before this gate.
The two Gate E review findings are closed with fresh direct verification and clean
independent review. Local Phase 17.75 closure is ready; Git integration, successful
standard Gradle execution in a normal environment/PR CI and Phase 18 remain
separately authorized work.

## Repository and inventory

- Branch/head/base: `codex/phase-17-75-contract-admission` /
  `d3a219767b2a16c6f4e5f7514019c56ca9cf6cf7` /
  `d3a219767b2a16c6f4e5f7514019c56ca9cf6cf7`.
- Opening status: 4 tracked status entries and 100 untracked paths before this
  Gate E record was created. The tracked
  set is `.gitattributes`, `tools/build.gradle`, and the two excluded quarantine
  files. Two accidental Blender `__pycache__/*.pyc` outputs were identified and
  removed. The final inventory is 100 intended Phase 17.75 untracked paths plus
  the excluded dist ZIP.
- Intended new-path categories after generated-file cleanup: 36 production tool
  Java files, 36 Java test/helper files, 4 Blender tests, 4 Blender production
  scripts/docs, 1 model-viewer README, 15 documentation paths and 4 resources
  (2 viewer shaders plus 2 inspector dependency/license resources).
- Ignored local evidence remains only under module/root build directories and is
  not an integration input. The staged area is empty.

## Exact A to D ownership replay

The real Blender 5.1.2 session ran the existing `repeated_exports` Gate C path in
an ephemeral `GAIA_GATE_C_SCENE` / `GAIA_ASSET_WORKSPACE`. It created only the
project-owned ruler and simple-tool probes, exported each twice, released every
captured fixture reference and did not save a `.blend`. The bounded structural
fingerprint before/after was identical:

`c5367e05e059cf92c26532bf7c86cb0b152a376ab58e677ad8121b825e49b52a`.

Environment and exporter evidence:

- Blender `5.1.2`; official `io_scene_gltf2` exporter `5.1.20`.
- exporter entrypoint SHA-256
  `dddd7d2362b937f34a5d4a1978259317720a9bc647bb3b4dc113f9d5c38cbf56`.
- Gaia exporter script SHA-256
  `f8361f4428f66383e6664a13fd6d5dd417f236e0f64aed61c69cbdd26e29f849`.
- pinned settings SHA-256
  `8de28c214af7d165409720215e59d5d06051f423541faf32cf5bc00f798f81f8`.

Exact same-state exports:

| fixture | bytes | GLB SHA-256 | repeat | Gate B report SHA-256 | result |
|---|---:|---|---|---|---|
| ruler | 2,192 | `359c1fba2c1411ab2874f560e85569fd29a80133ff101373b7ea6edf05ccdb4f` | byte-identical | `ebdfd2a1faba44cd07aa43cb0dd7d326492691c9f6b992d09d278df6e8b12627` | PASS |
| simple tool | 3,924 | `902e992711fec6d895ab7e1cc0a875a12f65164d33dbd11fb4960f8e1561d752` | byte-identical | `9aa40c40561ad492164f87e349d8801c174f1cf9f39b7326ac63f90e4cdb840b` | PASS |

Four separate headless Java adapter processes consumed the exact staged bytes.
Each preserved the Gate B JSON bytes and produced an exact-hash-bound validated
receipt with `asset_approval=NOT_GRANTED`. The ruler reports 24 primitive-domain
vertices/12 triangles and dimensions approximately `1.0 x 0.02 x 0.02`; the
simple tool reports 48 vertices/24 triangles and dimensions approximately
`0.04 x 0.24 x 0.025`. No Blender/MCP code participated in validation.

The Gate D viewer then opened the exact simple-tool hash only through
`ModelViewerMain -> GaiaGlbValidator -> ValidatedModelSnapshot -> ViewerCpuModel`.
The observed Windows context was NVIDIA GeForce RTX 5070 Laptop GPU, driver
573.24, OpenGL 4.1 and GLSL 4.10. The human verified model visibility, orbit, pan,
zoom, grid/bounds/wireframe toggles, resize, focus recovery and a valid reload.
After the target was replaced with the known invalid 1,012-byte GLB, the user
confirmed that CURRENT remained visible; counters remained current=1, candidate=0,
handles=6 and estimatedBytes=1,824. Restoring the exact valid tool GLB returned a
Gate B PASS with the expected source hash, and the user confirmed recovery and a
clean close. This smoke is direct human evidence, not inferred from automated input.

## Verification

- Direct independent headless JUnit launcher, full modelInspector source set:
  310 found/started/succeeded, 0 failed/aborted/skipped.
- Direct independent headless JUnit launcher, full modelViewer source set:
  62 found/started/succeeded, 0 failed/aborted/skipped.
- Direct ordinary tools suite: 55 found/started, 54 succeeded, 0 failed,
  1 existing abort.
- Blender pure Python suite: 14 run, 13 passed, 1 skipped because the OS did not
  permit local symlink creation.
- Fresh `javac --release 17` compilation on JDK 21.0.11: 17 inspector and 19
  viewer sources passed; generated class major is 61.
- Local verification JAR contains `ModelViewerMain` and both viewer shaders;
  both shaders are `#version 410 core`.
- Independent-process export/report comparison proves canonical Gate B JSON bytes
  are identical for each same-state export pair.
- Six dependency JAR hashes and the exact 1,079-byte JglTF MIT license match the
  admitted provenance receipt.

The required standard wrapper commands were each attempted once. All failed in
the single-use daemon bootstrap before Gradle task execution or test discovery
with `java.io.IOException: Unable to establish loopback connection`; this is the
same previously diagnosed host limitation:

- `:tools:modelInspectorTest`: INFRASTRUCTURE BLOCK.
- `:tools:modelViewerTest`: INFRASTRUCTURE BLOCK.
- `:tools:check`: INFRASTRUCTURE BLOCK.
- `clean test build`: INFRASTRUCTURE BLOCK.

These are not reported as PASS or as repository test failures. Phase 17.75 is not
merge-ready until these standard tasks execute successfully in a normal clean
environment or CI. The direct JVM evidence above establishes local code behavior,
not a substitute claim that the Gradle tasks passed.

## Boundaries and platform

- modelInspector resolves only the six admitted JglTF/Jackson runtime artifacts.
- modelViewer adds modelInspector plus the minimum engine/JOML/LWJGL substrate.
- ordinary tools, engine and game do not receive JglTF/Jackson/modelViewer.
- Viewer source contains no raw JglTF/Jackson/ImageIO/preflight/buffer decode path;
  it consumes only validated snapshots. No game resource packages the viewer
  shaders.
- No new third-party dependency, runtime GLB support, model registry, game
  renderer, Blender add-on, `.blend` source asset or gameplay chisel replacement.
- Windows JDK 21/headless and real Blender/Viewer evidence are PASS as enumerated.
  Java source/bytecode release 17 is PASS. Real JDK 17, Linux and macOS execution
  are NOT RUN.

## Final review and decision

Four Gate E read-only reviews covered end-to-end authority, hostile-resource and
GPU boundaries, build/dependency/license isolation, and scope/evidence/documentation.
Build/dependency/license review was clean. The evidence/documentation review found
two documentation-only inventory/handoff gaps; this record and the handoff correct
them without changing production/test/resource code.

Final reviewer counts after the documentation recheck are:

- End-to-end authority: 0 Critical / 0 Important / 0 Minor. Its initial receipt
  concern was retracted after checking the exact Gate E scope and authoring
  provenance.
- Security/resource boundary: 0 Critical / 2 Important / 0 Minor.
- Build/dependency/license: 0 Critical / 0 Important / 0 Minor.
- Scope/evidence/documentation: 0 Critical / 0 Important / 0 Minor after the two
  documentation-only corrections.

Main-agent source verification retained two new Important findings from the
security/resource review:

1. strict admission does not reject JSON `null` elements inside primitive-backed
   glTF numeric arrays before Jackson can map them to zero-valued DTO elements;
2. HAND_TOOL_V0 has no declared texture/sampler count limits, while the validated
   snapshot preserves every declared texture and the viewer allocates a sampler
   for every snapshot texture, including unused declarations.

Both are outside Gate E's one-cycle allowance because they require Gate A/B strict
mapping or HAND_TOOL_V0/resource-profile changes. No fix was attempted. Because
Important is nonzero, the conditional skeptical review was not run. Gate E remains
blocked for controller resolution even though the end-to-end replay and human smoke
passed.

## Controller-authorized contract closure

The controller subsequently froze two corrections without reopening unrelated
Gate A/B semantics:

- core numeric/integer/index arrays reject null elements before lossy DTO/default
  mapping, while optional whole-property and bounded `extras` nulls remain valid;
- HAND_TOOL_V0 hard limits are 8 declared textures and 8 declared samplers,
  counting unused declarations before snapshot projection. Existing limits remain
  8 materials and 4 images.

RED evidence on unchanged production code: the direct Inspector run found 324 and
failed exactly 9 new expectations (five primitive numeric-array null cases and four
resource-budget families); the direct Viewer run found 63 and failed its one new
Gate-B-profile projection test. The index-array null cases were already rejected by
the pinned boxed DTO path. After the minimal mapper/profile/count-order correction,
the first direct GREEN runs were Inspector 324/324 and Viewer 63/63. Final
verification, independent review and end-to-end evidence follow this record.

## Final controller-closure verification

Fresh compilation used an empty ignored output directory and `javac --release 17`
for all 17 Inspector and 19 Viewer production sources plus their complete tests.

- Inspector: 324 found/started/succeeded, 0 failed/aborted/skipped.
- Viewer: 63 found/started/succeeded, 0 failed/aborted/skipped.
- Ordinary tools: 55 found/started, 54 succeeded, 0 failed, 1 pre-existing abort.
- Two independent headless CLI processes produced byte-identical canonical JSON
  for the exact simple-tool GLB; both exited 0 and reported its unchanged SHA/PASS.
- The exact ruler GLB also exited 0/PASS with unchanged SHA and statistics.
- All six JglTF/Jackson runtime JAR byte hashes and the packaged JglTF MIT license
  hash match the admitted receipt. Fresh classes remain Java major 61.

All four standard wrapper commands were attempted after the source correction:
`:tools:modelInspectorTest`, `:tools:modelViewerTest`, `:tools:check` and
`clean test build`. Each stopped in single-use daemon startup before task execution
or discovery with `Unable to establish loopback connection`. Classification is
HOST LOOPBACK INFRASTRUCTURE BLOCK, never PASS. Normal environment/PR CI execution
is mandatory before merge.

The exact validated simple-tool GLB launched the real Windows OpenGL 4.1 Viewer
through Gate B. It published current=1/candidate=0, six handles and 1,824 estimated
bytes on the NVIDIA RTX 5070 Laptop GPU. A standard WM_CLOSE request then exercised
normal cleanup and the unified launch process returned exit 0. Gate C/D production
code and Blender scene state were unchanged.

Independent closure review results:

- Strict admission: 0 Critical / 0 Important / 0 Minor.
- Resource profile: 0 Critical / 0 Important / 0 Minor.
- Integration/evidence: 0 Critical / 0 Important / 1 documentation Minor before
  this final evidence was appended; the documentation-only correction changes no
  code, contract or test result.
- Final static skeptical review: `NO ADDITIONAL EVIDENCE-BACKED FINDING`.

Deduplicated technical total is 0 Critical / 0 Important / 0 Minor after the
documentation recheck. The one controller-directed correction cycle is complete.
No staging, commit, push, PR, merge or Phase 18 work occurred.
