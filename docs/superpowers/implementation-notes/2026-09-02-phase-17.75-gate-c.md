# Gate 17.75C — bounded Blender authoring/export evidence

Status: CLOSED / READY FOR CONTROLLER APPROVAL. Required evidence, independent
reviews and final safety audit complete; Gates A/B controller-approved; D/E NOT
authorized. Intermediate pending records below are retained as execution history.
Baseline/HEAD: `d3a219767b2a16c6f4e5f7514019c56ca9cf6cf7` on existing branch
`codex/phase-17-75-contract-admission`. No Git integration authorized/performed.

## Opening audit and architecture

Read current contract, design, B notes/handoff, validator/CLI/build, ignore rules
and provenance before implementation. Blender Lab MCP audit found Windows Blender
5.1.2, Python3.13.9, official exporter5.1.20 and actual RNA. The existing scene was
unsaved/clean, Object mode, Cube selected/active; Cube/Camera/Light and Collection
are user-owned. No enabled custom glTF export hooks were found. No connectivity
spike, preferences or addon changes. Source inspection was read-only.

The dedicated GAIA_GATE_C_SCENE / GAIA_ASSET_WORKSPACE fixtures are newly created
data API references, with explicit ownership tags and no linked/nested data.
Selection never defines export scope. All output-affecting RNA is pinned; Y-up
uses meters; no universal working axis. The scalar untextured material probe is
deliberately smaller than the full profile; optional texture export is NOT RUN.
No mesh/material/image semantic rules are reimplemented in Blender.

Task dependency refinement: fixture creation (Task3 first part) preceded Task2's
transaction tests because real export-failure tests need an owned source fixture.
Each API still received missing-API RED before implementation. No batching of
unverified production features or Gate B changes.

## RED / GREEN evidence

1. Pure helper suite: missing `gaia_export_glb` module RED (ModuleNotFoundError),
   then canonical receipt/settings/path/hash tests GREEN. Eight tests initially,
   seven passed plus one OS symlink-permission skip.
2. Real fixture tests: absent `gate_c_fixtures.py` RED (FileNotFoundError before
   any scene write). Initial fixture implementation exposed localized new-node
   names: Chinese UI creates a translated Principled display name. Read-only
   evidence identified it; stable `bl_idname` lookup corrected the creation path.
   Both ruler/tool dimensions/ownership/UV checks GREEN; cleanup restored exact
   before fingerprint even on the failed creation attempt.
3. Real transaction tests: absent `export_workspace` AttributeError RED. Minimal
   ownership/config/context/finally implementation GREEN for missing workspace,
   path rejection, unowned root rejection and injected exporter exception. No
   exception produced a successful receipt. Original UI/scene fingerprint matches.
4. Real repeated export exposed upstream logging behavior: explicit nonnegative
   loglevel leaves missing `loglevel` key in official exporter5.1.20. Inspected
   source confirmed its working `-1` auto-log path; only that setting changed.
   No installed code or preferences modified. Retry in a new run directory
   exported both fixtures twice; all original state restored.
5. Standalone adapter tests: absent `validate_export` module RED. Minimal Java
   subprocess + exact hash/exit binding GREEN for success, semantic FAIL,
   process failure/timeout and file mutation during process. Parent-traversal
   regression initially reached a nonexistent outside file (FileNotFoundError)
   instead of rejecting the ambiguous path; explicit pre-read path rejection
   made it GREEN. No outside file was created/read successfully.

No test assertion was weakened. Historical failed staging directories are retained
as ignored local evidence; no user-owned files were deleted. One initial MCP tool
call was stopped by stale GateC-not-authorized context. It did not execute. The
current explicit authorization and missing-module/pre-mutation test boundary were
verified before the authorized test ran; that interruption is not counted as RED.

## Real acceptance / exact identity

See `docs/model-authoring-provenance.md` for all exact hashes, actual bounds and
statistics. Local acceptance run is `tools/build/model-inspector/staging/gate-c/
acceptance-002`, with before/RNA/failure/export evidence in sibling `evidence/`.

- Ruler: 2192 bytes; 24 unique/expanded vertices, 12 triangles; actual meter-scale
  bounds agree with independent expected values within 1e-6m; both Gate B PASS.
- Tool: 3924 bytes; 48 unique/expanded vertices, 24 triangles; actual dimensions
  ~(.04,.24,.025)m; both Gate B PASS. Rigid translation/rotation exercised.
- Exactly POSITION/NORMAL/TEXCOORD_0; no images/extensions; expected object names
  only, so original Cube/Camera/Light are not in the GLBs.
- Two export calls per fixture have identical GLB/export-receipt/report/bound-
  receipt bytes despite different temporary selections. No GLB byte rewriting.
- Rejection-001: own ruler root scale2, Gate B SCENE_INVALID / FAIL / exit1.
  Export initially PENDING; bound receipt is FAIL, never artistic approval.

Independent process (four successes and one deliberate rejection):
`java -Djava.awt.headless=true -cp <isolated classpath> com.gaia.tools.model.ModelInspectorMain --json <exact GLB>`.
The Python adapter uses the existing compiled source set/resources and six
already-admitted JARs; no Blender/MCP calls. User Blender session remains running.

## Fresh verification

```powershell
python -B -m unittest discover -s tools/blender/tests -p 'test_*.py' -v
python -B tools/blender/tests/verify_staged_exports.py tools/build/model-inspector/staging/gate-c/acceptance-002
.\gradlew.bat :tools:modelInspectorTest --rerun-tasks --console=plain --no-daemon
.\gradlew.bat :tools:check --rerun-tasks --console=plain --no-daemon
```

Actually used bundled Python3.13.9. Current pure suite13 tests:12 passed/1 skip
(local OS did not permit symlink creation), zero failures/errors. Acceptance
checker asserts exact SHA/report/settings/bound receipt identity, actual bounds,
counts and exported attribute/object roster. These are acceptance assertions,
not an alternative validator.

Inspector task exit0,13s,4 tasks; tools:check exit0,47s,19 tasks. Final XML:
Inspector310 passed/0 failures/errors/skips; ordinary tools54 passed/1 pre-existing
skip/0 failures/errors. Java17 release compile/dependency bytecode, Gate A strict
preflight, image boundary, snapshot, dependency/license/hash and independent
headless JVM JSON equality are included. Deterministic UI/brand/hero/typography
resource checks GREEN. Existing deprecation warnings only. Initial sandbox cache
lock permission failure was environmental, not a test RED; authorized cache access
then ran both wrapper tasks. No full engine/game test claim or 5-hour suite.

Windows Blender5.1.2, Windows JDK21/headless PASS. `--release 17` PASS; real JDK17,
Blender other OS, Linux/macOS validator NOT RUN. Python tests not silently counted
as tools:check tests; no new CI dependency on Blender.

## Safety evidence / remaining review

Structural before/after SHA-256 for fixture tests, failure transactions, repeated
exports and deliberately invalid fixture:
`caef133ee26333bb28f0b7e3e44db753c3e9eb7b3245013d2d35035b32321008`.
The audit includes scene/collection tree, membership, transforms, material slots,
mesh identities/counts/topology samples, scene settings and active/selected/mode.
Original Cube's topology is fully covered by the bounded sample. No `.blend`
save, global preference change or unrelated mutation observed.

Four independent review seats and skeptical review: PENDING. Automatic review
closure cycles used:0/2. No gate completion claim until reviews/final safety audit.

## Independent primary review and bounded closure cycle 1

All four read-only seats completed before any correction:

- Safety:0C/0I/1M (link test did not reach guard).
- Export contract:0C/1I/0M (effective OBJECT material-slot override not checked).
- Provenance:0C/0I/1M (same link-test issue; deduplicated).
- End-to-end:0C/0I/0M additional findings.

Main independently verified both findings. Pinned export_apply=false uses
object.material_slots in the official exporter, while the wrapper checked
mesh.materials. Supplied fixtures use DATA slots, so no prior export contained
unrelated material. Minimal authoring restriction: reject any non-DATA slot.
No new semantic validation or HAND_TOOL_V0 rule.

Permanent real Blender regression uses a newly copied fixture material only:
OBJECT override reached the injected exporter boundary before fix (AssertionError,
RED); after the two-line precondition it rejects before invocation (GREEN).
No user material was borrowed/exported. Finally cleaned only captured data and
matched the original before/after fingerprint.

Minor closure is test-only: real-link test directly exercises _no_links; a new
valid-run-name test covers both symlink/junction guard branches using mocked link
observations. A separate in-memory counterfactual disabled _no_links: both
assertions failed as expected. The actual unmodified guard passes. Physical
symlink-creation test still honestly skips on this Windows permission boundary.
No junction/link privilege workaround or production guard change.

Fresh pure tests:14 total,13 passed/1 skip,0 failures/errors. All five real Blender
families reran: fixtures, transaction failures, override rejection, two exports per
fixture, scaled rejection. Every before/after fingerprint equals the original.
Four new standalone Gate B CLI passes and scaled rejection/exit1 were produced.
Final runs acceptance-003/rejection-002 bind the new script hash. Both GLB and
Gate B report hashes are unchanged; receipts correctly changed for the new script.
Exact-byte acceptance checker GREEN. Historical run002 is not final-script proof.

Cycle used:1/2. Relevant independent closure re-review and final skeptical review
pending; no gate completion claim yet.

Post-correction wrapper reruns: Inspector exit0/13s/4 tasks; tools:check
exit0/45s/19 tasks. Final XML again records310 Inspector passed, ordinary tools
54 passed/1 existing skip, zero failures/errors. Headless independent-JVM equality,
image/Gate A/dependency/bytecode/license/hash/snapshot checks included. Current
Python suite14=13passed/1permission skip. No new Java/dependency/source changes.

Independent scoped closure re-reviews completed: Safety0C/0I/0M;
Export-contract0C/0I/0M; Provenance0C/0I/0M. The end-to-end seat had no additional
finding; its mandatory new-script evidence has been regenerated and independently
rehash-checked. No reviewer finding was rejected; the duplicate Minor was counted
once. Static skeptical review is running; no Blender mutation/execution by reviewers.

## Gate C file inventory and future integration suggestions

New files (11):

- tools/blender/gaia_export_glb.py
- tools/blender/gate_c_fixtures.py
- tools/blender/validate_export.py
- tools/blender/README.md
- tools/blender/tests/test_export_helpers.py
- tools/blender/tests/test_validate_export.py
- tools/blender/tests/blender_export_checks.py
- tools/blender/tests/verify_staged_exports.py
- docs/model-authoring-provenance.md
- docs/superpowers/plans/2026-09-02-phase-17.75-gate-c.md
- docs/superpowers/implementation-notes/2026-09-02-phase-17.75-gate-c.md

Existing Gate A/B documentation updated (2): phase-17.75 handoff and design spec,
current gate status only; historical A/B records preserved. No contract change.
All 48 other opening intended files and the existing dist remain byte-identical;
the opening roster has51 files including dist and two modified status docs.

Entire Phase17.75 intended inventory now61 files (2 tracked diffs +59 untracked),
separate from two quarantined tracked modifications and pre-existing dist ZIP.
Tracked-only `git diff --stat`: .gitattributes +2; tools/build.gradle +50;
2files/+52/-0. Those are prior Gate A/B changes, not Gate C changes, and this
stat excludes all untracked files. Untracked whitespace scan:0. Forbidden output
candidate scan:0. The test-only `D:/outside` string is a synthetic rejection input,
not a local runtime path. Operational scene/RNA outputs are correctly ignored.

Suggested FUTURE commit: `feat(tools): add bounded Blender GLB authoring workflow`.
Suggested FUTURE PR title: `feat(tools): establish verified Blender export workflow`.
Suggested body: isolated ephemeral owned fixtures; explicit official exporter;
exact provenance and independent Gate B acceptance; scene restoration; verified
same-environment repeated exports; no blend save, viewer, runtime or new dependency.
These are documentation suggestions only. No stage/commit/push/PR/merge authorized.

## Final skeptical review and closure

One fresh strictly static skeptical reviewer read current code, final receipts,
before/after evidence, Gate B reports and tests. It returned exactly:
**NO ADDITIONAL EVIDENCE-BACKED FINDING**. No Blender call, script/test/probe,
download, repository edit or Git operation was performed by that reviewer.

Final verified findings: **0 Critical /0 Important /0 Minor**. One additional
automatic correction cycle used out of2. No Gate B issue, profile change, new
dependency, viewer, runtime asset, engine/game modification or Gate D work.

All workflow families retained the original structural SHA-256. Final repeated
export evidence has saved=false/dirty=false; original active/selected Cube and
Object mode remain. No `.blend` save call. Protected SHA-256:

- quarantine metrics: EBBAAD69942CE0F8504BD37DA1B2AF7878BCC19EA38D21A27C788B4C1F7ABB51
- quarantine integration test: D8C6D9DCC01707809C6414AFA9F11C4D2501CD30748A145897E8F65BA594782C
- dist ZIP: FC7D521BD7DCFBC3142E0153C82A0F63FFFC2301483FE119EBB1D372F35228D2
- unchanged index: 2E3BC1AD0CFBC3B91F62D63A4964622CBB59BFAE2A6143DFF0E511E6E59E6809

`git diff --check` and untracked whitespace/artifact audit PASS. Staged area empty;
HEAD/cached origin/main remain original baseline, divergence0/0. No fetch, stage,
commit, push, PR, merge or worktree operation in this gate. Generated GLBs/reports
remain ignored, no user-owned file deletion. Gate A/B source/tests/resources/build
hashes match opening state. Historical and final evidence remain distinguishable.

Windows Blender and JDK21 headless PASS; release17/dependency bytecode PASS;
real JDK17, Linux/macOS execution NOT RUN. Optional texture authoring/export not
exercised; no cross-platform byte determinism, artistic or runtime approval claim.

**GATE 17.75C — CLOSED / READY FOR CONTROLLER APPROVAL**

STOP. Do not begin Gate17.75D without separate controller authorization.
