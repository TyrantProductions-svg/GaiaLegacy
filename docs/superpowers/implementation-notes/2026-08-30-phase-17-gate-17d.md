# Phase 17 Gate 17D implementation notes

## Scope

Gate 17D adds bounded HUD projection and closes verification/documentation over
the accepted Gate 17A-C architecture. It adds no interaction, mutation,
inventory, WorldItem, mesh, renderer, save, crafting, or progression authority.

## Task 20 - HUD

- RED: `DetailHudPresentationTest` failed to compile because the immutable
  current DETAIL projection did not exist.
- GREEN: `BlockInteractionSnapshot` and `HudPresentationSnapshot` now project
  the selected material, Survival matching-unit count, local quarter, preview
  validity/reason, mode, and one current failure.
- `DetailToolWidget` uses existing text rendering. Creative omits unit count;
  focus/blocking lifecycle clears the projection. There is no history.

## Task 21 - feedback

The Gate 17B APPLIED callback path already supplied exact quarter feedback, so
no production behavior was fabricated. New regression coverage proves four
localized remove debris particles, three placement particles, deterministic
seeds, and zero success feedback without a committed callback. The existing
atlas resolver supplies held chisel presentation. Sound remains deferred.

## Task 22 - persistence and streaming

Production integration coverage performs an atomic sculpt in an actual
production session, captures it, restores a fresh session, and verifies exact
DETAIL state with no serialized preview, plus restored raycast, collision, and
installed mesh presentation. Additional production-composed scenarios execute
the real Survival precision-recovery transaction and real uniform coarse-break
transaction before fresh-session restore, proving chisel/detail-unit inventory
identity and the one-FULL-block WorldItem result survive the same canonical
save path. A repository lifecycle test prepares, acknowledges, and commits a
real unload, verifies non-residency, then restores the detached canonical
capture exactly. Existing streamed `detail-blocks`, inventory, and WorldItem
codecs remain unchanged.

## Task 23 - rapid edits

`DetailEditPerformanceFixture` uses real `GaiaDetailMutationService`,
`ChunkRepository`, `ChunkMeshBuilder`, and `ChunkMeshManager` lifecycle calls.
It retains only aggregate counters and the bounded set of affected Chunk keys.
There is no per-edit history or gameplay retry. Current completions and stale
completion rejection are measured separately.

Representative result:

```text
attempts=120 applied=120 stale=0 rejected=0
totalLatencyNanos=8,398,500 maximumLatencyNanos=6,246,200
affectedChunks=[ChunkKey[x=0,z=0]] finalRevision=121 finalOccupiedCount=0
meshAcceptedPeak=1 meshActivePeak=1 meshCompletedPeak=1
staleMeshResults=1 meshOutputBytesPeak=1440
meshCpuBudgetBytesPeak=4320 meshCpuBudgetBytesLimit=134217728
meshAffectedChunks=1 meshBuildLatencyNanos=276700
```

The figures include fixture/resource cold start and are evidence, not a product
threshold. Existing mesh budget tests remain the authority for 32 accepted, 2
active, and 128 MiB accounting.

## Final acceptance

Fresh proportional evidence is GREEN: engine 658/658, game 842/842, the
independent save/streaming matrix 642/642, and tools 33 passed / 1 skipped.
Packaged resources, packaged shaders, installed shaders/audio, and
`installDist` are GREEN. The save/streaming matrix completed in 10h16m45s; its
expensive Phase 14 migration and 1,024-owner WorldItem cases remained RUNNABLE
and advanced normally rather than deadlocking.

The first tools-wide run exposed a genuine fixture RED: the isolated
`BlockIconGeneratorTest` resource pack copied the production resource index but
not its three newly indexed standalone item definitions. Adding those three
JSON files to the isolated JAR restored the focused test and complete tools
suite without changing production behavior or generated icon geometry.

The first final repository-wide wrapper run then exposed a second genuine
resource RED after all Java tests and packaging had passed:
`tools:verifyGeneratedUiAssets` detected that the checked-in Phase 17 chisel
cell was not reproducible by `BlockIconGenerator`. The generator still knew
only block-backed icons. A TDD correction taught the existing generator to
resolve `gaia:chisel` through the canonical standalone `itemVisual` seam and
render its `ATLAS_REGION`; it did not reintroduce `blockForItem()` or another
item authority. Focused generator/resource tests, the complete tools suite,
and deterministic generated-asset verification are GREEN. The corrected icon
atlas SHA-256 is
`732A12F9815ADFD3332B67466240A908481B29B845132A58CE2DE4966D57FA75` and
metadata SHA-256 is
`91B27613C0C45B523EDEABC982A81B30AB8B34729605264AA6AC41AF326DEDB4`.

The packaged Windows GLFW process launched and exited cleanly with exit 0.
Windows.Graphics.Capture exposed only the OpenGL swap-chain background, and the
Computer Use adapter could not provide valid visual acceptance evidence. The
user subsequently completed the full production Windows GLFW/OpenGL matrix
manually, and the controller accepted Gate 17D. Reported PASS coverage includes
HUD/tool readability, Creative and Survival DETAIL transactions, conservation
and inventory-full behavior, uniform and partial/mixed coarse rules, rapid
edits, real unload/return, Save & Quit and fresh-process restore, focus recovery,
resize alignment, ordinary FULL regressions, and clean exit. No unreported
numeric runtime values are inferred from that manual result.

After the final review corrections, the fresh repository-wide wrapper command
`gradlew.bat clean test build --console=plain --no-daemon` is GREEN (exit 0,
5h38m6s, 31/31 tasks executed): engine 1505/1505, game 2275/2275, and tools
34 passed / 1 skipped, with zero failures/errors. Packaged resources, generated
UI assets, packaged/installed shaders and audio, `installDist`, and all module
build tasks completed in that same run.

Final independent review is 0 Critical / 0 Important / 0 Minor. Gates 17A-D
are controller-accepted and Windows interactive acceptance is `PASS`. Native
Apple Silicon interactive runtime remains `NOT RUN` unless actual hardware is
used.

The full wrapper remains intentionally expensive: the final run took 5h38m6s,
and the broad save/streaming matrix took 10h16m45s because it includes existing
heavy migration, staging, and streaming scenarios. This is not classified as a
Phase 17 correctness regression. Future engineering may audit Gradle test tiers
such as fast, integration, heavy, and release without deleting or weakening
the heavy release coverage.

## Deferred

The `gaia:chisel` atlas visual is an `ACCEPTED PLACEHOLDER`; its canonical item
identity and `DETAIL_PRECISION` capability remain presentation-independent.
Gameplay chisel SFX is `DEFERRED` because no suitable bounded committed SFX seam
exists.

Phase 18 owns normal acquisition, crafting-lite/fabrication, FULL to/from 64
units, tool and harvest tiers, progression, and balance. Its documented future
asset path is: audit/select Blender MCP; install its add-on/server; configure a
localhost-only Codex connection; produce and human-review a low-poly draft;
preserve `.blend`; export GLB; integrate Gaia runtime presentation; inspect in
Gaia Model Inspector; human-tune held transforms; and apply the approved
configuration. Phase 19 owns deterministic natural DETAIL generation through
the same canonical representation and systems, without generated/player
provenance and with a future generated-detail density budget. No deferred work
is implemented here.
