# Gate 17.5C focused visual correction

Approved user direction: smooth project-owned emblem and atmospheric ringed planet only.
Preserve A+ layout, navigation, fonts, motion, HUD, secondary screens and gameplay.

## Ordered TDD tasks

1. Brand generator: add failing tests for a 256-square transparent RGBA page,
   partial-alpha smooth curves, padded edge RGB and repeatable output. Implement
   project-owned vector/path geometry, 4x deterministic supersampling and box
   downsample. Keep canonical source in tools, no external artwork.
2. Runtime brand binding: failing loader/presenter tests for one LINEAR brand page
   and one textured emblem quad. Extend existing texture ownership/map and package
   inventory only; retain typography page sampling and existing hit regions.
3. Hero generator: failing tests for filled sphere, tilted elliptical rings and
   opaque geometric front/back occlusion before atmospheric compositing. Bake the
   generated layer into all three existing hero variants. Keep topo motif unchanged.
4. Run focused tools/game/engine UI tests and DPI fixtures. Run generators in two
   independent JVM processes and compare all bytes with repository asset outputs.
   Verify packaged UI/shader resources and installDist as relevant.
5. Real Windows GLFW review: capture corrected main menu at 1280x720-equivalent
   and resized desktop size; report actual DPI only. Do not infer visual PASS.
6. Record provenance, byte/draw/bind costs, tests and screenshots. Audit diff,
   quarantined hashes and dist. STOP for human review; no Git integration.

Focused command pattern (process-local known JDK selector fallback only):
`./gradlew.bat :tools:test --tests '*RuntimeBrandAssetGeneratorTest' --console=plain --no-daemon`
Then corresponding `RuntimeHeroAssetGeneratorTest`, `GaiaUiAssetLoaderTest`,
`ProductScreenPresenterTest`, `ProductUiDpiMatrixTest`, and packaged contracts.

No runtime SVG/TTF, second renderer, procedural runtime planet, external asset,
gameplay change, staging, commit or push is authorized.
