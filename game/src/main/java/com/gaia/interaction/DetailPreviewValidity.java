package com.gaia.interaction;

/** Bounded read-only validity classification for a precision preview. */
public enum DetailPreviewValidity {
    VALID,
    OCCUPIED,
    UNKNOWN,
    FAILED,
    OUT_OF_BOUNDS
}
